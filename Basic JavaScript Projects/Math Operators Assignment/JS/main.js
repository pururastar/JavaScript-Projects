function performMathOperation() {
    // Perform a simple addition operation
    let result = 5 + 3;
    
    // Display the result in the paragraph with id "Math"
    document.getElementById("Math").innerHTML = "The result of the operation is: " + result;
}
