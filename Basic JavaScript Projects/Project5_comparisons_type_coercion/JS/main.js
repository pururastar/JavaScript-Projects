// Define a variable of your choice
var myVariable = 42;

// Use typeof to get the data type of the variable
var dataType = typeof myVariable;

// Display the data type in the HTML document
document.getElementById("dataType").textContent = dataType;
